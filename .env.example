# AI 识图 API 配置（二选一或都配，优先使用 AI_PROVIDER 指定的服务）

# 选择提供商: openai | dashscope
AI_PROVIDER=openai

# OpenAI（GPT-4o 视觉）
OPENAI_API_KEY=sk-your-key-here
OPENAI_MODEL=gpt-4o

# 阿里云通义千问（Qwen-VL）
DASHSCOPE_API_KEY=sk-your-key-here
DASHSCOPE_MODEL=qwen-vl-max
